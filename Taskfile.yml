version: "3"

vars:
  UV: '{{default "uv" .UV}}'
  UV_PYTHON: '{{default "python3.13" .UV_PYTHON}}'
  DEV_VENV_DIR: '{{default ".venv" .DEV_VENV_DIR}}'
  DEV_OVERRIDES: '{{default "requirements-dev-overrides.txt" .DEV_OVERRIDES}}'
  IMAGE_REGISTRY: '{{default "10.10.10.10:5000" .IMAGE_REGISTRY}}'
  IMAGE_NAMESPACE: '{{default "" .IMAGE_NAMESPACE}}'
  IMAGE_TAG: '{{default "latest" .IMAGE_TAG}}'
  DEV_IMAGE_REGISTRY: '{{default "localhost:5001" .DEV_IMAGE_REGISTRY}}'
  DEV_IMAGE_NAMESPACE: '{{default "" .DEV_IMAGE_NAMESPACE}}'
  DEV_IMAGE_TAG: '{{default "dev" .DEV_IMAGE_TAG}}'
  DOCKER_BUILD_PLATFORMS: '{{default "linux/amd64,linux/arm64" .DOCKER_BUILD_PLATFORMS}}'
  DOCKER_BUILD_CACHE_FROM: '{{default "" .DOCKER_BUILD_CACHE_FROM}}'
  DOCKER_BUILD_CACHE_TO: '{{default "" .DOCKER_BUILD_CACHE_TO}}'
  APP_SERVICES: auth-service concert-service notification-service payment-service reservation-service ticket-service
  APP_IMAGE_SERVICES: auth-service concert-service reservation-service payment-service ticket-service notification-service
  DEV_SERVICE_PYPROJECTS: services/auth-service/pyproject.toml services/concert-service/pyproject.toml services/reservation-service/pyproject.toml services/payment-service/pyproject.toml services/ticket-service/pyproject.toml services/notification-service/pyproject.toml
  DASHBOARD_SERVICE: '{{default "dashboard" .DASHBOARD_SERVICE}}'

includes:
  tests:
    taskfile: ./tests/Taskfile.yml
    dir: .

tasks:
  default:
    desc: Show available service commands.
    cmds:
      - task --list

  help:
    desc: Show available service commands.
    cmds:
      - task --list

  list:
    desc: Show available service commands.
    cmds:
      - task --list

  install:
    desc: Sync the unified uv development environment.
    cmds:
      - |
        set -e
        uv_cmd='{{.UV}}'
        python_spec='{{.UV_PYTHON}}'
        venv_dir='{{.DEV_VENV_DIR}}'
        overrides='{{.DEV_OVERRIDES}}'
        if ! command -v "${uv_cmd}" >/dev/null 2>&1; then
          printf '%s\n' "${uv_cmd} command not found. Install uv or set UV=/path/to/uv." >&2
          exit 1
        fi
        "${uv_cmd}" venv --python "${python_spec}" "${venv_dir}" --allow-existing
        venv_python="${venv_dir}/bin/python"
        if [ ! -x "${venv_python}" ]; then
          venv_python="${venv_dir}/Scripts/python.exe"
        fi
        install_args=""
        for project in {{.DEV_SERVICE_PYPROJECTS}}; do
          install_args="${install_args} -r ${project} --group ${project}:test"
        done
        "${uv_cmd}" pip install --python "${venv_python}" --exact --strict --overrides "${overrides}" ${install_args}
        printf 'Unified uv development environment is ready at %s.\n' "${venv_dir}"

  test-runner-build:
    desc: Build service-scoped Docker Python test runner images. Optionally set SERVICE=<service-name>.
    cmds:
      - task: tests:test-runner-build
        vars:
          SERVICE: '{{default "" .SERVICE}}'

  test-service:
    desc: Run one service pytest in its Docker Python runner with SERVICE=<service-name>.
    cmds:
      - task: tests:test-service
        vars:
          SERVICE: '{{default "" .SERVICE}}'

  test-services:
    desc: Run selected service pytest suites in parallel with service-scoped Docker runners.
    cmds:
      - task: tests:test-services
        vars:
          SERVICES: '{{default "" .SERVICES}}'

  test-unit:
    desc: Run all managed service pytest suites in service-scoped Docker runners.
    cmds:
      - task: tests:test-unit

  test:
    desc: Run the default test target.
    cmds:
      - task: tests:test-unit

  test-all:
    desc: Run unit tests and Docker Compose E2E tests.
    cmds:
      - task: tests:test-unit
      - task: tests:test-e2e

  test-e2e:
    desc: Run Docker Compose E2E tests. Optionally set SCENARIO=<scenario-name>.
    cmds:
      - task: tests:test-e2e
        vars:
          SCENARIO: '{{default "" .SCENARIO}}'

  benchmark-concert-public-api:
    cmds:
      - task: tests:benchmark-concert-public-api
        vars:
          CONCERTS: '{{default "1000" .CONCERTS}}'
          SHOWTIMES_PER_CONCERT: '{{default "4" .SHOWTIMES_PER_CONCERT}}'
          SEATS_PER_SHOWTIME: '{{default "100" .SEATS_PER_SHOWTIME}}'
          SAMPLES: '{{default "50" .SAMPLES}}'
          WARMUP: '{{default "5" .WARMUP}}'
          TEST_RUNNER_IMAGE_PREFIX: '{{default "ticketing-test-runner" .TEST_RUNNER_IMAGE_PREFIX}}'

  benchmark-api-smoke:
    desc: Run smoke API integration benchmarks for managed services.
    cmds:
      - task: tests:benchmark-api-smoke
        vars:
          PRESET: '{{default "smoke" .PRESET}}'
          SAMPLES: '{{default "5" .SAMPLES}}'
          WARMUP: '{{default "1" .WARMUP}}'
          API_BENCHMARK_REPORT_ROOT: '{{default "tests/tmp/reports/api-integration" .API_BENCHMARK_REPORT_ROOT}}'
          PYTEST_ARGS: '{{default "-q -s -p no:cacheprovider" .PYTEST_ARGS}}'

  benchmark-api-smoke-service:
    cmds:
      - task: tests:benchmark-api-smoke-service
        vars:
          SERVICE: '{{default "" .SERVICE}}'
          PRESET: '{{default "smoke" .PRESET}}'
          SAMPLES: '{{default "5" .SAMPLES}}'
          WARMUP: '{{default "1" .WARMUP}}'
          API_BENCHMARK_REPORT_ROOT: '{{default "tests/tmp/reports/api-integration" .API_BENCHMARK_REPORT_ROOT}}'
          PYTEST_ARGS: '{{default "-q -s -p no:cacheprovider" .PYTEST_ARGS}}'

  benchmark-api-large:
    desc: Run large API integration benchmarks for managed services.
    cmds:
      - task: tests:benchmark-api-large
        vars:
          PRESET: '{{default "half-year-early-growth" .PRESET}}'
          SAMPLES: '{{default "100" .SAMPLES}}'
          WARMUP: '{{default "2" .WARMUP}}'
          API_BENCHMARK_REPORT_ROOT: '{{default "tests/tmp/reports/api-integration" .API_BENCHMARK_REPORT_ROOT}}'
          PYTEST_ARGS: '{{default "-q -s -p no:cacheprovider" .PYTEST_ARGS}}'

  benchmark-api-large-service:
    cmds:
      - task: tests:benchmark-api-large-service
        vars:
          SERVICE: '{{default "" .SERVICE}}'
          PRESET: '{{default "half-year-early-growth" .PRESET}}'
          SAMPLES: '{{default "100" .SAMPLES}}'
          WARMUP: '{{default "2" .WARMUP}}'
          API_BENCHMARK_REPORT_ROOT: '{{default "tests/tmp/reports/api-integration" .API_BENCHMARK_REPORT_ROOT}}'
          PYTEST_ARGS: '{{default "-q -s -p no:cacheprovider" .PYTEST_ARGS}}'

  benchmark-api-report:
    desc: Generate service API integration benchmark Markdown reports from local artifacts.
    cmds:
      - task: tests:benchmark-api-report
        vars:
          SERVICE: '{{default "all" .SERVICE}}'
          API_BENCHMARK_REPORT_ROOT: '{{default "tests/tmp/reports/api-integration" .API_BENCHMARK_REPORT_ROOT}}'
          API_BENCHMARK_DOC_DIR: '{{default "../workspace/docs/evidence/services/api-integration-test-benchmark/half-year-early-growth" .API_BENCHMARK_DOC_DIR}}'

  app-images-build:
    desc: Build service and dashboard Docker images.
    cmds:
      - |
        set -eu
        image_registry='{{.IMAGE_REGISTRY}}'
        image_namespace='{{.IMAGE_NAMESPACE}}'
        image_tag='{{.IMAGE_TAG}}'
        dashboard='{{.DASHBOARD_SERVICE}}'
        if [ -z "${image_registry}" ]; then
          printf '%s\n' 'IMAGE_REGISTRY is required, for example: task app-images-build IMAGE_REGISTRY=localhost:5001 IMAGE_TAG=dev' >&2
          exit 2
        fi
        if [ -z "${image_tag}" ]; then
          printf '%s\n' 'IMAGE_TAG is required, for example: task app-images-build IMAGE_REGISTRY=localhost:5001 IMAGE_TAG=dev' >&2
          exit 2
        fi
        image_prefix="${image_registry}"
        if [ -n "${image_namespace}" ]; then
          image_prefix="${image_registry%/}/${image_namespace#/}"
        fi
        build_platforms='{{.DOCKER_BUILD_PLATFORMS}}'
        if [ -z "${build_platforms}" ]; then
          printf '%s\n' 'DOCKER_BUILD_PLATFORMS is required.' >&2
          exit 2
        fi
        cache_args=""
        cache_from='{{.DOCKER_BUILD_CACHE_FROM}}'
        cache_to='{{.DOCKER_BUILD_CACHE_TO}}'
        while IFS= read -r cache; do
          if [ -n "${cache}" ]; then
            cache_args="${cache_args} --cache-from ${cache}"
          fi
        done <<EOF
        ${cache_from}
        EOF
        while IFS= read -r cache; do
          if [ -n "${cache}" ]; then
            cache_args="${cache_args} --cache-to ${cache}"
          fi
        done <<EOF
        ${cache_to}
        EOF
        build_image() {
          service="$1"
          printf 'building %s/%s:%s\n' "${image_prefix}" "${service}" "${image_tag}"
          case "${service}" in
            auth-service|concert-service|reservation-service|payment-service|ticket-service|notification-service)
              docker buildx build ${cache_args} --platform "${build_platforms}" --push -f "services/${service}/Dockerfile" -t "${image_prefix}/${service}:${image_tag}" .
              ;;
            "${dashboard}")
              docker build -t "${image_prefix}/${service}:${image_tag}" "${dashboard}"
              ;;
            *)
              docker build -t "${image_prefix}/${service}:${image_tag}" "services/${service}"
              ;;
          esac
        }
        pids=""
        for service in {{.APP_IMAGE_SERVICES}}; do
          build_image "${service}" &
          pids="${pids} $!"
        done
        build_image "${dashboard}" &
        pids="${pids} $!"
        failed=0
        for pid in ${pids}; do
          if ! wait "${pid}"; then
            failed=1
          fi
        done
        if [ "${failed}" -ne 0 ]; then
          exit 1
        fi

  app-images-push:
    desc: Build images and push them to the registry.
    deps:
      - app-images-build
    cmds:
      - |
        set -eu
        image_registry='{{.IMAGE_REGISTRY}}'
        image_namespace='{{.IMAGE_NAMESPACE}}'
        image_tag='{{.IMAGE_TAG}}'
        dashboard='{{.DASHBOARD_SERVICE}}'
        image_prefix="${image_registry}"
        if [ -n "${image_namespace}" ]; then
          image_prefix="${image_registry%/}/${image_namespace#/}"
        fi
        pids=""
        for service in {{.APP_IMAGE_SERVICES}} ${dashboard}; do
          (
            printf 'pushing %s/%s:%s\n' "${image_prefix}" "${service}" "${image_tag}"
            docker push "${image_prefix}/${service}:${image_tag}"
          ) &
          pids="${pids} $!"
        done
        failed=0
        for pid in ${pids}; do
          if ! wait "${pid}"; then
            failed=1
          fi
        done
        if [ "${failed}" -ne 0 ]; then
          exit 1
        fi

  app-image-build:
    desc: Build one service or dashboard Docker image with SERVICE=<image-name>.
    cmds:
      - |
        set -eu
        requested='{{default "" .SERVICE}}'
        image_registry='{{.IMAGE_REGISTRY}}'
        image_namespace='{{.IMAGE_NAMESPACE}}'
        image_tag='{{.IMAGE_TAG}}'
        dashboard='{{.DASHBOARD_SERVICE}}'
        if [ -z "${requested}" ]; then
          printf '%s\n' 'SERVICE is required, for example: task app-image-build SERVICE=concert-service IMAGE_REGISTRY=localhost:5001 IMAGE_TAG=dev' >&2
          exit 2
        fi
        if [ -z "${image_registry}" ]; then
          printf '%s\n' 'IMAGE_REGISTRY is required, for example: task app-image-build SERVICE=concert-service IMAGE_REGISTRY=localhost:5001 IMAGE_TAG=dev' >&2
          exit 2
        fi
        if [ -z "${image_tag}" ]; then
          printf '%s\n' 'IMAGE_TAG is required, for example: task app-image-build SERVICE=concert-service IMAGE_REGISTRY=localhost:5001 IMAGE_TAG=dev' >&2
          exit 2
        fi
        normalize_service() {
          raw="$1"
          if [ "${raw}" = "${dashboard}" ]; then
            printf '%s\n' "${raw}"
            return 0
          fi
          for candidate in {{.APP_IMAGE_SERVICES}}; do
            if [ "${raw}" = "${candidate}" ]; then
              printf '%s\n' "${candidate}"
              return 0
            fi
            if [ "${raw}-service" = "${candidate}" ]; then
              printf '%s\n' "${candidate}"
              return 0
            fi
          done
          return 1
        }
        if ! service="$(normalize_service "${requested}")"; then
          printf 'Unknown SERVICE=%s (expected one of: {{.APP_IMAGE_SERVICES}} %s).\n' "${requested}" "${dashboard}" >&2
          exit 2
        fi
        image_prefix="${image_registry}"
        if [ -n "${image_namespace}" ]; then
          image_prefix="${image_registry%/}/${image_namespace#/}"
        fi
        build_platforms='{{.DOCKER_BUILD_PLATFORMS}}'
        if [ -z "${build_platforms}" ]; then
          printf '%s\n' 'DOCKER_BUILD_PLATFORMS is required.' >&2
          exit 2
        fi
        cache_args=""
        cache_from='{{.DOCKER_BUILD_CACHE_FROM}}'
        cache_to='{{.DOCKER_BUILD_CACHE_TO}}'
        while IFS= read -r cache; do
          if [ -n "${cache}" ]; then
            cache_args="${cache_args} --cache-from ${cache}"
          fi
        done <<EOF
        ${cache_from}
        EOF
        while IFS= read -r cache; do
          if [ -n "${cache}" ]; then
            cache_args="${cache_args} --cache-to ${cache}"
          fi
        done <<EOF
        ${cache_to}
        EOF
        build_output='{{.DOCKER_BUILD_OUTPUT}}'
        build_output_arg='--push'
        if [ -n "${build_output}" ]; then
          build_output_arg="--output ${build_output}"
        fi
        printf 'building %s/%s:%s\n' "${image_prefix}" "${service}" "${image_tag}"
        case "${service}" in
          auth-service|concert-service|reservation-service|payment-service|ticket-service|notification-service)
            docker buildx build ${cache_args} --platform "${build_platforms}" ${build_output_arg} -f "services/${service}/Dockerfile" -t "${image_prefix}/${service}:${image_tag}" .
            ;;
          "${dashboard}")
            docker build -t "${image_prefix}/${service}:${image_tag}" "${dashboard}"
            ;;
          *)
            docker build -t "${image_prefix}/${service}:${image_tag}" "services/${service}"
            ;;
        esac

  app-image-push:
    desc: Build and push one service or dashboard Docker image with SERVICE=<image-name>.
    cmds:
      - task app-image-build SERVICE={{.SERVICE}} IMAGE_REGISTRY={{.IMAGE_REGISTRY}} IMAGE_NAMESPACE={{.IMAGE_NAMESPACE}} IMAGE_TAG={{.IMAGE_TAG}}
      - |
        set -eu
        requested='{{default "" .SERVICE}}'
        image_registry='{{.IMAGE_REGISTRY}}'
        image_namespace='{{.IMAGE_NAMESPACE}}'
        image_tag='{{.IMAGE_TAG}}'
        dashboard='{{.DASHBOARD_SERVICE}}'
        normalize_service() {
          raw="$1"
          if [ "${raw}" = "${dashboard}" ]; then
            printf '%s\n' "${raw}"
            return 0
          fi
          for candidate in {{.APP_IMAGE_SERVICES}}; do
            if [ "${raw}" = "${candidate}" ]; then
              printf '%s\n' "${candidate}"
              return 0
            fi
            if [ "${raw}-service" = "${candidate}" ]; then
              printf '%s\n' "${candidate}"
              return 0
            fi
          done
          return 1
        }
        if ! service="$(normalize_service "${requested}")"; then
          printf 'Unknown SERVICE=%s (expected one of: {{.APP_IMAGE_SERVICES}} %s).\n' "${requested}" "${dashboard}" >&2
          exit 2
        fi
        image_prefix="${image_registry}"
        if [ -n "${image_namespace}" ]; then
          image_prefix="${image_registry%/}/${image_namespace#/}"
        fi
        printf 'pushing %s/%s:%s\n' "${image_prefix}" "${service}" "${image_tag}"
        docker push "${image_prefix}/${service}:${image_tag}"

  deploy:tag:
    desc: Create and push a deploy tag with SERVICE=<service|changed|all> BUMP=<patch|minor|major>. Set DRY_RUN=true to preview.
    cmds:
      - bash scripts/deploy_tag.sh
    env:
      SERVICE: '{{default "" .SERVICE}}'
      BUMP: '{{default "patch" .BUMP}}'
      DRY_RUN: '{{default "false" .DRY_RUN}}'
      DEPLOY_ENV: '{{default "dev" .DEPLOY_ENV}}'

  dev-images-build:
    desc: Build Docker Desktop dev images with DEV_IMAGE_* variables.
    cmds:
      - |
        set -eu
        local_docker_build_platform() {
          arch="$(uname -m 2>/dev/null || true)"
          case "${arch}" in
            arm64|aarch64)
              printf '%s\n' 'linux/arm64'
              return 0
              ;;
            x86_64|amd64)
              printf '%s\n' 'linux/amd64'
              return 0
              ;;
          esac
          docker_arch="$(docker info --format '{{"{{"}}.Architecture{{"}}"}}' 2>/dev/null || true)"
          case "${docker_arch}" in
            arm64|aarch64)
              printf '%s\n' 'linux/arm64'
              return 0
              ;;
            x86_64|amd64)
              printf '%s\n' 'linux/amd64'
              return 0
              ;;
          esac
          printf 'Unsupported local build architecture: uname=%s docker=%s\n' "${arch}" "${docker_arch}" >&2
          return 2
        }
        dev_build_platform="$(local_docker_build_platform)"
        printf 'Using local Docker build platform: %s\n' "${dev_build_platform}"
        task app-images-build \
          IMAGE_REGISTRY={{.DEV_IMAGE_REGISTRY}} \
          IMAGE_NAMESPACE={{.DEV_IMAGE_NAMESPACE}} \
          IMAGE_TAG={{.DEV_IMAGE_TAG}} \
          DOCKER_BUILD_PLATFORMS="${dev_build_platform}"

  dev-images-push:
    desc: Push Docker Desktop dev images with DEV_IMAGE_* variables.
    cmds:
      - |
        set -eu
        local_docker_build_platform() {
          arch="$(uname -m 2>/dev/null || true)"
          case "${arch}" in
            arm64|aarch64)
              printf '%s\n' 'linux/arm64'
              return 0
              ;;
            x86_64|amd64)
              printf '%s\n' 'linux/amd64'
              return 0
              ;;
          esac
          docker_arch="$(docker info --format '{{"{{"}}.Architecture{{"}}"}}' 2>/dev/null || true)"
          case "${docker_arch}" in
            arm64|aarch64)
              printf '%s\n' 'linux/arm64'
              return 0
              ;;
            x86_64|amd64)
              printf '%s\n' 'linux/amd64'
              return 0
              ;;
          esac
          printf 'Unsupported local build architecture: uname=%s docker=%s\n' "${arch}" "${docker_arch}" >&2
          return 2
        }
        dev_build_platform="$(local_docker_build_platform)"
        printf 'Using local Docker build platform: %s\n' "${dev_build_platform}"
        task app-images-push \
          IMAGE_REGISTRY={{.DEV_IMAGE_REGISTRY}} \
          IMAGE_NAMESPACE={{.DEV_IMAGE_NAMESPACE}} \
          IMAGE_TAG={{.DEV_IMAGE_TAG}} \
          DOCKER_BUILD_PLATFORMS="${dev_build_platform}"
